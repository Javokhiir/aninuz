/**
 * Services runs on the house palette — see `lib/house-theme.ts` for why the
 * blue rather than the products section's Inmarco red. Re-exported under the
 * section's own names so the components read like the products ones.
 */
export {
  HOUSE_THEME as SERVICES_THEME,
  houseThemeVars as servicesThemeVars,
} from "@/lib/house-theme"
